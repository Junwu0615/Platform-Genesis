{
  "name": "source-cp-mach-order",
  "config": {
    "connector.class": "io.confluent.connect.mqtt.MqttSourceConnector",

    "confluent.topic.bootstrap.servers": "pg-cluster-kafka-1:29092",
    "confluent.index.bootstrap.servers": "pg-cluster-kafka-1:29092",
    "mqtt.server.uri": "tcp://pg-cluster-mqtt-1:1883",
    "mqtt.username": "admin",
    "mqtt.password": "123456789",

    "tasks.max": "3",
    "mqtt.qos": "1",
    "mqtt.clean.session.exists": "true",
    "value.converter.schemas.enable": "false",

    "confluent.topic.replication.factor": "1",
    "confluent.license.topic.replication.factor": "1",
    "topic.creation.default.replication.factor": "1",

    "topic.creation.default.partitions": "72",
    "topic.creation.default.compression.type": "lz4",

    "mqtt.topics": "cp/mach-order/+",
    "kafka.topic": "source.cp.mach-order",

    "key.converter": "org.apache.kafka.connect.storage.StringConverter",
    "value.converter": "org.apache.kafka.connect.converters.ByteArrayConverter"
  }
}