# ansible/playbook.yml


- name: Dynamic Monitoring Configuration
  hosts: monitoring_servers  # 對應 .ini 群組
  become: false              # root 開關，視情況調整
  roles:
    - monitoring
  vars:
    config_path: "{{ playbook_dir }}/../docker/monitoring/prometheus.yaml"