{
  "name": "sink-inst-prod-records",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "connection.url": "jdbc:postgresql://dev-db:5432/pgdatabase",
    "connection.user": "olap_user",
    "connection.password": "olap_pwd",
    "auto.create": "false",
    "auto.evolve": "false",

    "tasks.max": "3",

    "topics": "inst.prod-records",
    "table.name.format": "oltp.production_records",

    "insert.mode": "insert",
    "pk.mode": "none",

    "key.converter.schemas.enable": "false",
    "value.converter": "org.apache.kafka.connect.json.JsonConverter",
    "value.converter.schemas.enable": "true",

    "transforms": "ReplaceField",
    "transforms.ReplaceField.type": "org.apache.kafka.connect.transforms.ReplaceField$Value",
    "transforms.ReplaceField.whitelist": "order_id,machine_id,product_id,quantity,event_time"
  }
}