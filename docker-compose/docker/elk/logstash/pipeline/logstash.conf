## --- 第一階段：數據從哪裡來 ---
#input {
#  # 接收來自 Beats (如 Filebeat) 的資料
#  beats {
#    port => 5044
#  }
#
#  # 接收來自 HTTP 的 JSON 資料 (測試用)
#  http {
#    port => 5000
#    codec => json
#  }
#}
#
## --- 第二階段：數據怎麼加工 ---
#filter {
#  # 範例：如果是 Nginx 日誌，可以自動拆解欄位
#  # grok {
#  #   match => { "message" => "%{COMBINEDAPACHELOG}" }
#  # }
#
#  # 增加一個自定義欄位
#  mutate {
#    add_field => { "cluster_name" => "my_elk_cluster" }
#  }
#}
#
## --- 第三階段：數據往哪裡去 ---
#output {
#  # 傳送到 Elasticsearch
#  elasticsearch {
#    hosts => ["http://elasticsearch:9200"]
#    index => "logstash-%{+YYYY.MM.dd}"
#    user => "elastic"
#    password => "change_me_please" # 建議從環境變數讀取
#  }
#
#  # 同時在終端機印出來 (方便除錯，生產環境建議刪除)
#  stdout {
#    codec => rubydebug
#  }
#}